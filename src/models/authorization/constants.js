export const EMPTY_OBJECT = {};

export const LOGIN = {
  endpoint: "/authenticate",
};

export const SIGNUP = {
  endpoint: "/registerUser",
};

export const LOGOUT = {
  endpoint: "/logout",
};

export const FETCH_USER = {
  endpoint: "/user",
};
